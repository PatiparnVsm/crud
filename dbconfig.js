const config = {
    user : 'sa',
    password : 'P@ssw0rd',
    server : 'localhost',
    database :'Products',
    options:{
        trustedConnection: true,
        enableArithAort: true,
    }
}

module.exports = config;